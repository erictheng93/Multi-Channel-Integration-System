# 消息搜索功能用戶指南

**版本**: 1.0.0 (Phase 1-3 Complete)
**最後更新**: 2025-10-08
**適用對象**: 客服人員、系統管理員、最終用戶

---

## 📚 目錄

- [快速開始](#快速開始)
- [基礎搜索](#基礎搜索)
- [高級搜索](#高級搜索)
- [搜索歷史](#搜索歷史)
- [搜索技巧](#搜索技巧)
- [常見問題](#常見問題)
- [快捷鍵](#快捷鍵)

---

## 🚀 快速開始

### 第一次使用搜索

1. **打開會話詳情頁面**
   - 進入任意客戶會話
   - 查看右上角的搜索框

2. **執行第一次搜索**
   ```
   步驟 1: 點擊搜索框
   步驟 2: 輸入關鍵詞（例如："訂單"）
   步驟 3: 按 Enter 或等待 0.3 秒自動搜索
   步驟 4: 查看搜索結果，關鍵詞會高亮顯示
   ```

3. **清除搜索**
   - 點擊搜索框右側的 ❌ 圖標
   - 或者刪除所有文字後按 Enter

### 搜索模式

系統提供兩種搜索模式：

| 模式 | 按鈕標識 | 適用場景 |
|------|----------|----------|
| **標準模式 (STD)** | STD | 日常快速搜索 |
| **高級模式 (ADV)** | ADV | 複雜條件搜索 |

**切換方式**: 點擊搜索框左側的 STD/ADV 按鈕

---

## 🔍 基礎搜索

### 單關鍵詞搜索

最簡單的搜索方式，直接輸入要查找的詞語。

**示例**:
```
輸入: 訂單
結果: 包含"訂單"的所有消息
```

**適用場景**:
- 查找特定主題的消息
- 快速定位關鍵詞
- 日常搜索需求

### 多關鍵詞搜索

輸入多個詞語，系統會找到包含任一詞語的消息。

**示例**:
```
輸入: 訂單 配送
結果: 包含"訂單"或"配送"的所有消息
```

**適用場景**:
- 查找相關主題
- 擴大搜索範圍
- 探索性搜索

### 完全匹配搜索

使用引號包圍詞語，查找完全匹配的短語。

**示例**:
```
輸入: "訂單已發貨"
結果: 僅包含完整短語"訂單已發貨"的消息
```

**適用場景**:
- 查找特定消息
- 精確匹配
- 避免誤匹配

### 中英文搜索

系統同時支持中文和英文搜索。

**中文示例**:
```
輸入: 客戶服務
結果: 包含"客戶"或"服務"的消息
```

**英文示例**:
```
輸入: customer service
結果: 包含"customer"或"service"的消息
```

**混合示例**:
```
輸入: VIP 客戶
結果: 包含"VIP"或"客戶"的消息
```

---

## ⚡ 高級搜索

點擊 **ADV** 按鈕切換到高級搜索模式，使用強大的搜索語法。

### 布爾運算符

#### AND（並且）

查找同時包含多個詞語的消息。

**語法**: `詞語1 AND 詞語2`

**示例**:
```
輸入: 訂單 AND 完成
結果: 同時包含"訂單"和"完成"的消息
✓ "您的訂單已完成"
✓ "訂單處理完成，感謝您"
✗ "訂單正在處理中"（無"完成"）
✗ "配送已完成"（無"訂單"）
```

**適用場景**:
- 縮小搜索範圍
- 精確查找
- 組合條件搜索

#### OR（或者）

查找包含任一詞語的消息（默認行為，可省略 OR）。

**語法**: `詞語1 OR 詞語2`

**示例**:
```
輸入: 退款 OR 取消
結果: 包含"退款"或"取消"的消息
✓ "申請退款"
✓ "訂單已取消"
✓ "取消後退款"（兩者都有）
```

**適用場景**:
- 擴大搜索範圍
- 查找相關主題
- 同義詞搜索

#### NOT（排除）

排除包含特定詞語的消息。

**語法**: `詞語1 NOT 詞語2`

**示例**:
```
輸入: 問題 NOT 解決
結果: 包含"問題"但不包含"解決"的消息
✓ "遇到了問題"
✓ "問題還在處理中"
✗ "問題已解決"
✗ "解決方案"（有"解決"）
```

**適用場景**:
- 過濾無關結果
- 排除已處理項目
- 精煉搜索結果

#### 組合運算符

使用括號組合多個運算符。

**語法**: `(詞語1 OR 詞語2) AND 詞語3`

**示例**:
```
輸入: (訂單 OR 產品) AND 完成
結果: 包含"完成"且包含"訂單"或"產品"的消息
✓ "訂單已完成"
✓ "產品配送完成"
✗ "訂單處理中"（無"完成"）
✗ "配送完成"（無"訂單"或"產品"）
```

**複雜示例**:
```
輸入: (退款 OR 取消) AND (訂單 NOT 完成)
結果: 包含"退款"或"取消"，且包含"訂單"但不包含"完成"的消息
```

### 通配符搜索

#### 前綴匹配

查找以特定詞語開頭的所有變體。

**語法**: `詞語*`

**示例**:
```
輸入: 產品*
結果: 包含以"產品"開頭的詞語
✓ "產品經理"
✓ "產品詳情"
✓ "產品規格"
✗ "新產品"（"產品"不在開頭）
```

**適用場景**:
- 查找詞語變體
- 不確定完整詞語
- 前綴匹配

#### 後綴匹配

查找以特定詞語結尾的所有變體。

**語法**: `*詞語`

**示例**:
```
輸入: *配送
結果: 包含以"配送"結尾的詞語
✓ "快速配送"
✓ "免費配送"
✓ "上門配送"
✗ "配送中"（"配送"不在結尾）
```

### 字段搜索

僅在特定字段中搜索。

#### 搜索消息內容

**語法**: `content:關鍵詞`

**示例**:
```
輸入: content:訂單
結果: 僅在消息內容中搜索"訂單"
✓ 消息: "您的訂單已發貨"
✗ 發送者: "訂單客服"（僅匹配發送者名稱）
```

#### 搜索發送者名稱

**語法**: `senderName:名稱`

**示例**:
```
輸入: senderName:客服
結果: 僅在發送者名稱中搜索"客服"
✓ 發送者: "客服小王"
✓ 發送者: "在線客服"
✗ 消息: "聯繫客服"（僅匹配消息內容）
```

#### 組合字段搜索

**示例**:
```
輸入: content:訂單 AND senderName:客服
結果: 消息內容包含"訂單"且發送者名稱包含"客服"
✓ 客服小李: "您的訂單已發貨"
✗ 客戶張三: "我的訂單呢？"（發送者不是客服）
```

**實用示例**:
```
# 查找客服關於退款的回復
輸入: content:退款 AND senderName:客服

# 查找客戶的問題消息
輸入: content:問題 AND senderName:客戶

# 查找特定客服的所有消息
輸入: senderName:小王
```

---

## 📜 搜索歷史

系統自動保存您的搜索記錄，方便快速重複搜索。

### 查看搜索歷史

1. **聚焦搜索框**
   - 點擊搜索框
   - 自動顯示最近搜索記錄（最多 10 條）

2. **查看完整歷史**
   - 搜索框中輸入部分關鍵詞
   - 顯示匹配的歷史記錄

### 使用搜索建議

**操作方式**:
```
步驟 1: 點擊搜索框
步驟 2: 在建議列表中找到需要的搜索
步驟 3: 點擊建議項目自動填充
步驟 4: 按 Enter 執行搜索
```

**示例**:
```
歷史記錄:
  1. "訂單 AND 完成" - 25 條結果 - 2 分鐘前
  2. "退款" - 8 條結果 - 5 分鐘前
  3. "content:問題" - 42 條結果 - 10 分鐘前
```

### 智能排序

搜索歷史按以下規則排序：

1. **使用頻率** - 經常搜索的詞語排在前面
2. **最近時間** - 最近搜索的排在前面
3. **結果數量** - 有結果的搜索優先顯示

### 管理搜索歷史

**自動管理**:
- 最多保存 50 條歷史記錄
- 超過限制時自動刪除最舊的記錄
- 重複搜索會更新時間戳

**手動管理**:
- 暫不支持手動刪除單條記錄
- 清除瀏覽器數據會清空所有歷史

---

## 💡 搜索技巧

### 提高搜索準確性

#### 1. 使用具體詞語

**❌ 不好的搜索**:
```
問題
→ 結果太多，不夠精確
```

**✅ 好的搜索**:
```
訂單配送問題
→ 結果更精確
```

#### 2. 使用 AND 縮小範圍

**❌ 不好的搜索**:
```
訂單
→ 包含所有訂單相關消息
```

**✅ 好的搜索**:
```
訂單 AND 取消
→ 僅顯示取消的訂單
```

#### 3. 使用 NOT 排除干擾

**❌ 不好的搜索**:
```
問題
→ 包含已解決和未解決的問題
```

**✅ 好的搜索**:
```
問題 NOT 解決
→ 僅顯示未解決的問題
```

### 常見搜索場景

#### 場景 1: 查找未解決的客戶問題

```
搜索: 問題 NOT 解決
說明: 找出所有包含"問題"但不包含"解決"的消息
用途: 快速識別待處理事項
```

#### 場景 2: 查找特定時間段的訂單

```
搜索: 訂單 AND (今天 OR 昨天)
說明: 找出最近的訂單消息
用途: 跟進最近訂單
```

#### 場景 3: 查找客服的回復

```
搜索: senderName:客服
說明: 找出所有客服發送的消息
用途: 檢查客服回復質量
```

#### 場景 4: 查找退款相關消息

```
搜索: (退款 OR 取消 OR 退貨) AND 訂單
說明: 找出所有退款相關的訂單消息
用途: 處理退款申請
```

#### 場景 5: 查找包含附件的消息

```
搜索: 圖片 OR 文件 OR 附件
說明: 找出包含附件的消息
用途: 查看客戶提供的證據
```

### 性能優化技巧

#### 1. 避免過於寬泛的搜索

**❌ 不推薦**:
```
的
→ 匹配幾乎所有消息，性能差
```

**✅ 推薦**:
```
訂單詳情
→ 具體詞語，搜索更快
```

#### 2. 使用字段搜索

**❌ 較慢**:
```
客服
→ 搜索所有字段
```

**✅ 更快**:
```
senderName:客服
→ 僅搜索發送者字段
```

#### 3. 利用搜索歷史

**❌ 較慢**:
```
每次都輸入完整搜索詞
```

**✅ 更快**:
```
從搜索建議中選擇
→ 避免重複輸入
```

---

## ❓ 常見問題

### Q1: 為什麼搜索沒有結果？

**可能原因**:
1. 關鍵詞拼寫錯誤
2. 使用了過於嚴格的 AND 條件
3. 消息中確實不包含該關鍵詞

**解決方法**:
```
嘗試 1: 檢查拼寫
嘗試 2: 使用 OR 代替 AND
嘗試 3: 使用更通用的關鍵詞
嘗試 4: 使用通配符 (例如: 訂單*)
```

### Q2: 搜索結果太多怎麼辦？

**解決方法**:
```
方法 1: 使用 AND 添加更多條件
  訂單 → 訂單 AND 配送

方法 2: 使用 NOT 排除無關結果
  問題 → 問題 NOT 解決

方法 3: 使用字段搜索
  客服 → senderName:客服

方法 4: 使用完全匹配
  訂單 → "訂單已發貨"
```

### Q3: 如何搜索特殊字符？

**回答**: 系統會自動處理特殊字符，直接搜索即可。

**示例**:
```
搜索: @用戶名
搜索: #標籤
搜索: $價格
→ 自動處理，正常搜索
```

### Q4: 搜索區分大小寫嗎？

**回答**: 不區分。英文搜索不區分大小寫。

**示例**:
```
customer = Customer = CUSTOMER
→ 結果相同
```

### Q5: 為什麼有些消息沒有高亮？

**可能原因**:
1. 消息匹配的是組合條件，而非單個關鍵詞
2. 使用了 NOT 條件（排除的詞語不會高亮）

**示例**:
```
搜索: 問題 NOT 解決
結果: 包含"問題"的消息會高亮"問題"
      但不會高亮"解決"（因為是排除條件）
```

### Q6: 搜索歷史多久會過期？

**回答**: 搜索歷史永久保存，直到：
- 達到 50 條上限（自動刪除最舊的）
- 清除瀏覽器數據
- 手動清除緩存

### Q7: 搜索會卡頓嗎？

**回答**: 不會。系統已優化搜索性能：
- 防抖延遲：0.3 秒（避免頻繁搜索）
- 索引緩存：首次構建後緩存 7 天
- 背景處理：不阻塞界面
- 性能目標：<15ms 執行時間

### Q8: 可以搜索已刪除的消息嗎？

**回答**: 不可以。搜索僅針對當前存在的消息。

### Q9: 如何清除搜索？

**方法 1**: 點擊搜索框右側的 ❌ 圖標
**方法 2**: 刪除所有文字後按 Enter
**方法 3**: 刷新頁面

### Q10: 搜索支持正則表達式嗎？

**回答**: 不支持。系統使用 Lunr.js 全文索引，不支持正則表達式。

**替代方案**: 使用通配符 (*) 實現類似功能
```
正則: /訂單\d+/
替代: 訂單*
```

---

## ⌨️ 快捷鍵

### 搜索操作

| 快捷鍵 | 功能 |
|--------|------|
| `Ctrl/Cmd + F` | 聚焦搜索框 |
| `Enter` | 執行搜索 |
| `Esc` | 清除搜索 / 取消聚焦 |
| `↑` / `↓` | 在搜索建議中導航 |
| `Enter` (建議選中時) | 選擇建議並搜索 |

### 搜索模式切換

| 快捷鍵 | 功能 |
|--------|------|
| `Ctrl/Cmd + Shift + S` | 切換 STD/ADV 模式 |

**註**: 部分快捷鍵可能與瀏覽器衝突，請根據實際情況調整。

---

## 📚 附錄

### 搜索語法速查表

```
基礎搜索:
  訂單                    # 單關鍵詞
  訂單 配送               # 多關鍵詞 (OR)
  "訂單已發貨"            # 完全匹配

布爾運算:
  訂單 AND 完成           # 並且
  退款 OR 取消            # 或者
  問題 NOT 解決           # 排除
  (訂單 OR 產品) AND 完成 # 組合

通配符:
  產品*                   # 前綴匹配
  *配送                   # 後綴匹配

字段搜索:
  content:訂單            # 搜索內容
  senderName:客服         # 搜索發送者
```

### 性能指標

系統性能優化後的實際指標：

```
索引構建:
  1,000 條消息: ~8ms
  10,000 條消息: ~20ms

搜索執行:
  基礎搜索: ~6ms
  高級搜索: ~12ms
  模糊搜索: ~14ms

用戶體驗:
  搜索延遲: 300ms (防抖)
  結果顯示: 即時
  界面響應: 流暢無阻塞
```

### 瀏覽器支持

系統支持以下瀏覽器（最新版本）：

- ✅ Google Chrome 120+
- ✅ Mozilla Firefox 120+
- ✅ Apple Safari 17+
- ✅ Microsoft Edge 120+

**功能降級**:
- 私密瀏覽模式下，緩存功能自動降級為內存緩存
- 不支持 Web Worker 的瀏覽器會使用主線程索引

---

## 📞 獲取幫助

### 技術文檔
- 部署指南: `MESSAGE_SEARCH_DEPLOYMENT_GUIDE.md`
- 測試報告: `MESSAGE_SEARCH_TEST_REPORT.md`
- API 參考: 開發者文檔

### 聯繫支持
- 技術問題: 查看項目文檔 (`CLAUDE.md`)
- Bug 報告: GitHub Issues
- 功能建議: 產品團隊

---

**版本歷史**:
- v1.0.0 (2025-10-08) - Phase 1-3 完整實施
  - 基礎全文索引搜索
  - 高級搜索語法
  - 性能優化和監控

**下一版本計劃**:
- v1.1.0 - 搜索結果分頁 UI
- v1.2.0 - 高級過濾器（日期範圍、消息類型）
- v2.0.0 - AI 智能搜索

---

**最後更新**: 2025-10-08
**文檔維護**: 技術團隊
