# prod
dist/

# dev
.yarn/
!.yarn/releases
.vscode/*
!.vscode/launch.json
!.vscode/*.code-snippets
.idea/workspace.xml
.idea/usage.statistics.xml
.idea/shelf

# deps
node_modules/
.wrangler
bun.lockb  # Bun lockfile (混合環境階段暫不提交)
bun.lock   # Bun lockfile variant

# env - 所有環境變數檔案
.env
.env.local
.env.*.local
!.env.example
!.env.*.example
!.env.development
.env.terraform
.dev.vars
tests/.env
tests/.env.test

# logs
logs/
*.log

# temp test files
test-*.cjs
test-*.js
test-*.ts
!tests/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# misc
.DS_Store

# Terraform
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
tfplan
crash.log
crash.*.log
create_admin.sql
# DB rebuild seed files — contain credentials/seed data, never commit
seed-admin.sql
seed-settings.sql
"frontend/nul"  
frontend/nul 

# 測試和臨時文件
.playwright-mcp/
backups/*
!backups/README.md
monitoring/
archive/
nul
test-results/
test-results.json
coverage/
coverage-report.json
*verbose-output.txt

# 開發工具臨時檔案
frontend/.gemini-clipboard/
.gemini-clipboard/
.superpowers/
tmp-*

# Claude Code 個人本機設定（每位開發者獨立維護，不共享）
.claude/settings.local.json
.mcp.json

# Security reports / local audit artifacts (contains vulnerability details)
.gstack/

# 生成的 QR Code 檔案
team*_qr.svg
*_qr.svg

# Local staging rebuild artifacts (schema dump + seed w/ bootstrap creds)
staging-schema.sql
staging-seed.sql

# Local D1 mirror dump (prod PII: sessions/IPs/customer data) — see scripts/sync-d1-local.ts
d1-remote-dump.sql

# LINE backfill artifacts (customer PII / conversation content)
backfill-convos.json
backfill.sql
backfill-all.json
backfill-delta.sql
eric-msgs.json
eric-rebuild.sql
gen-eric.mjs

# Local debug artifacts (user PII / login screenshots with credentials)
User.xlsx
*-login-success.png
olivia-login-success.png

# Sync report artifacts (written at runtime by scripts/sync-database.ts)
sync-reports/*
!sync-reports/.gitkeep
