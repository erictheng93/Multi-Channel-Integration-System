# Cloudflare Pages 重定向規則
# SPA 路由支援 - 所有非 API 請求都重定向到 index.html
/*    /index.html   200

# API 代理到後端 Worker
# ⚠️ IMPORTANT: Configure this via Cloudflare Pages Functions or proxy settings
# The backend URL should be set in Cloudflare Dashboard
# /api/*  https://YOUR_BACKEND_URL/api/:splat  200