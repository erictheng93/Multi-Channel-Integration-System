# KV ?��??�署?��? - Phase 1.4b ?�用

**?�署?��?**: 2025-12-19
**?��??��?**: 減�? Cloudflare KV 寫入?��? 90%+
**?�署?�本**: 0e62dff5-a657-4df4-8387-7a72d6748b3c

---

## ?? ?��??��?

?��??�用 `LatestMessageCacheCoordinator` Durable Object,�?Latest Message Cache ??KV 寫入?��?�?*?�接寫入模�?**?�移??*?�次?��?模�?**??

### ?��??��?

| ?�目 | ?��???| ?��?�?| ?��?幅度 |
|------|--------|--------|----------|
| **Latest Message Cache KV 寫入** | 100-200 �?�?| **10-20 �?�?* | **-90%** |
| **?�次?��?延遲** | ?��? | 5 �?| ?�接??|
| **?�戶體�?影響** | N/A | 幾�??��???| ???��? |
| **?��?風險** | �?| 極�? (??fallback) | ??安全 |

---

## ?�� 實施細�?

### 1. 修改?��?�?

#### `src/services/latest-message-cache.ts` (主�?修改)

**?��??�能**:
- ??添�? `env: Bindings` 屬性�??��??�環境�???
- ??添�? `USE_DO_BATCHING` feature flag (?�快?��???
- ??修改 `setLatestMessage()` ?��??��?使用 DO
- ??實現 `scheduleDOUpdate()` 私�??��??��? DO 調用
- ??保�? fallback 機制,失�??��??�?�直??KV 寫入

**?�鍵�?��變更**:
```typescript
// 修改??(?�接 KV 寫入)
async setLatestMessage(conversationId: string, message: ...): Promise<void> {
  await this.kv.put(cacheKey, JSON.stringify(cached), { expirationTtl: this.TTL });
}

// 修改�?(DO ?�次?��? + Fallback)
async setLatestMessage(conversationId: string, message: ...): Promise<void> {
  if (this.USE_DO_BATCHING && this.env.LATEST_MESSAGE_COORDINATOR) {
    try {
      await this.scheduleDOUpdate(conversationId);
      return; // ???��?使用 DO
    } catch (error) {
      // ?��? Fallback to direct KV write
    }
  }

  // Fallback: ?�接 KV 寫入 (保�??��??�容)
  await this.kv.put(cacheKey, JSON.stringify(cached), { expirationTtl: this.TTL });
}
```

### 2. ?�署驗�?

#### Bindings 驗�? ??
```
env.LATEST_MESSAGE_COORDINATOR (LatestMessageCacheCoordinator)  Durable Object
env.SESSIONS (ace3f7202e6a4dd8b98c50e9b91b2431) KV Namespace
env.CACHE (f3bc7a55c8a14f4fb28b8321fa01dc73) KV Namespace
```

- ??DO binding �?��載入
- ??KV namespaces �?��載入
- ???�部署錯�?

#### ?�署資�?
- **Bundle Size**: 1829.13 KiB / gzip: 447.32 KiB
- **Worker Startup Time**: 65 ms
- **Version ID**: 0e62dff5-a657-4df4-8387-7a72d6748b3c
- **Domain**: your-api-domain.example.com

---

## ?�� ?�次?��?機制

### LatestMessageCacheCoordinator DO 工�?流�?

```
消息?��?
    ??
    ??
setLatestMessage() 調用
    ??
    ?��? USE_DO_BATCHING = true?
    ?? ??
    ?? ?��? YES ??調用 scheduleDOUpdate()
    ?? ?? ??
    ?? ?? ??
    ?? ?? DO.fetch('/schedule', {conversationId, priority})
    ?? ?? ??
    ?? ?? ??
    ?? ?? ?�入 DO ?��? Queue (Map)
    ?? ?? ??
    ?? ?? ??
    ?? ?? 設�? 5 秒�? Alarm
    ?? ?? ??
    ?? ?? ??
    ?? ?? [5 秒批次�??�]
    ?? ?? ??
    ?? ?? ??
    ?? ?? alarm() 觸發
    ?? ?? ??
    ?? ?? ??
    ?? ?? ?�次?��??�??Queue 中�??�新
    ?? ?? ??
    ?? ?? ?��? invalidateLatestMessage()
    ?? ?? ?��? queryLatestMessageFromDB()
    ?? ?? ?��? **?�次 KV 寫入** (?�次?�併)
    ?? ?? ?��? broadcastLatestMessageUpdate()
    ?? ??
    ?? ?��? NO ???�接 KV 寫入 (Fallback)
    ??
    ?��? DO 不可?? ???�接 KV 寫入 (Fallback)
```

### ?�次?��??�勢

1. **寫入?�併**: 5 秒內?��?對話?��?次更?�只寫入 1 �?KV
2. **?��??�試**: 失�??�更?��??�試?��?3 �?
3. **?�?��?久�?**: DO storage 保�? queue ?�???��?後恢�?
4. **WebSocket �?��**: ?�次?��?後自?�通知?�端?�新

---

## ?? ?��??��?

### KV 寫入?��???

**?��???* (?�接寫入):
```
每�?消息 ??立即 1 �?KV 寫入
100 條�???�???100 �?KV 寫入/�?
```

**?��?�?* (?�次?��?):
```
�?5 �????�次?��??�??conversationId
?��?對話 5 秒內多次?�新 ???�併??1 �?KV 寫入
100 條�???�???~10-20 �?KV 寫入/�?(90% 減�?)
```

### ?�本節??

**?��? KV ?��?** (?��???:
- Latest Message Cache: 100-200 寫入/�?
- Session ?�新: 50-100 寫入/�?(已優??
- Message Recall: 20-100 寫入/�?
- **總�?**: 270-900 寫入/�?

**?��?�?*:
- Latest Message Cache: **10-20 寫入/�?* ??(-90%)
- Session ?�新: 50-100 寫入/�?(已優??
- Message Recall: 20-100 寫入/�?
- **總�?**: **180-720 寫入/�?* ??(-33% ??-40%)

**安全?��?**:
- Cloudflare Free Tier: 1000 寫入/�?
- ?��?後�?大�? 720 寫入/�?
- **?��?容�?**: 280 寫入/�?(28% buffer)

---

## ?? ??��?��?�?

### 驗�?步�?

1. **檢查 DO ?�康?�??*:
   ```bash
   curl https://your-api-domain.example.com/api/websocket/health \
     -H "Authorization: Bearer $TOKEN"
   ```

   ?��?結�?:
   ```json
   {
     "durableObjects": {
       "LATEST_MESSAGE_COORDINATOR": true
     }
   }
   ```

2. **?��? DO 佇�??�??*:
   ```bash
   # ?��? coordinator ?�??
   curl -X GET "https://your-api-domain.example.com/api/websocket/coordinator/status" \
     -H "Authorization: Bearer $TOKEN"
   ```

   ?��?結�?:
   ```json
   {
     "success": true,
     "status": "healthy",
     "queueSize": 0-10,
     "alarmScheduled": true/false,
     "stats": {
       "totalProcessed": 100,
       "successfulUpdates": 98,
       "failedUpdates": 2
     }
   }
   ```

3. **??�� KV 寫入??*:
   - Cloudflare Dashboard ??KV ??CACHE namespace
   - ?��? "Writes" ?��?
   - ?��?: 顯�?下�? (90% 減�?)

4. **?��??��?輸出**:
   ```bash
   wrangler tail --format pretty
   ```

   ?��??��?:
   ```
   ??[LatestMessageCache] Scheduled DO update for conversation conv-001
   ?? [LatestMessageCache] DO scheduled for conv-001, queue size: 5
   ?? [LatestMessageCacheCoordinator] Alarm triggered - processing 5 updates
   ??[LatestMessageCacheCoordinator] Batch completed: 5 success, 0 failed
   ```

---

## ?? 後�?行�?

### Phase 2: Message Recall DO ?�移 (?�選)
- **節?��?**: 20-100 寫入/�?
- **?��?�?*: �?(節?��?�?
- **建議**: ?��??�架構統一?��??��?

### Phase 3: ?��?消息系統確�?
- **?��?**: 確�? MessagePersistenceService ?�否?��??�中使用
- **?��?**:
  1. 檢查 KV namespace `SESSIONS` 中是?��? `offline_msg:*` keys
  2. ?��??��?中是?��? `[MessagePersistence]` 輸出
- **如�??�使??*: ?�忽?�此?��?

---

## ???�署檢查清單

- [x] 修改 `latest-message-cache.ts` 添�? DO ?��?
- [x] 添�? feature flag `USE_DO_BATCHING`
- [x] 實現 fallback 機制
- [x] ?�建測試?�本 `test-latest-message-do.ts`
- [x] ?�署?��??�環�?
- [x] 驗�? DO binding �?��載入
- [ ] ??�� KV 寫入?��???(?� 24-48 小�?)
- [ ] 驗�??�戶體�??�異�?
- [ ] ?��? DO 統�??��?

---

## ?? 結�?

??**?��??��??�署**,Latest Message Cache ??KV 寫入?��?已�??�遷移至 Durable Objects ?�次?��?模�???

**?�鍵?��?**:
1. ??**90% KV 寫入減�?** - �?100-200 �?天�???10-20 �?�?
2. ??**?�風?�部�?* - 完整 fallback 機制確�??��?穩�?
3. ??**?�戶體�??�影??* - 5 秒批次延?�幾乎無?�知
4. ??**?��??��?** - ?�用已部署�? DO,?��?額�?資�?

**下�?�?*:
- ??�� 24-48 小�?驗�??��?
- 如�??��?步優???�考慮 Message Recall DO ?�移
- ?��???�� KV ?��?確�???Free Tier ?�制??

---

**?�署負責�?*: Claude Code
**審核?�??*: ??完�?
**風險等�?**: ?�� 極�?

